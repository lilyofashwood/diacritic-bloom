# Recovery receipt · 2026-09-10

The complete recovered Diacritic Bloom 1.0.0 source bundle is preserved here. It pairs a decorative Unicode styling codec with an exact JSON recipe format. The original literal specimen remains unchanged.

The publishing kit supplied this bundle under `sources/diacritic-bloom`; the original source/protocol/data checksums are retained in `SHA256SUMS.json`. The workspace archive also preserves the source kit. `index.html` is a new launch entry; no existing protocol or artwork was changed to make the new entry.

Authorship: Lily of Ashwood, with AI-assisted implementation and design. Released under the [MIT license](LICENSE), copyright © 2026 lilyofashwood. [Third-party notices](THIRD_PARTY_NOTICES.md) credit the public-domain Mulberry32 generator. `TESTING.md` preserves the earlier session's verification record.

Zalgo MUX 3.0.0 extends the separation between glyph bodies and mark layers into independent payload channels. Diacritic Bloom keeps its own identity: letter styling, mark growth and exact recipes.

Fresh verification on 2026-09-10: all 27 original Node tests pass; deterministic rebuild matches all 17 original SHA256 entries, including both generated artifacts. Readable source and tables were reviewed in full; the deterministic rebuild verified the generated embedded tables.

The Node `browser.test.cjs` harness passes exact specimen/font loading, Unicode and mixed-mode roundtrips, strict-error cleanup, recipe export/import, inert text, clipboard/fallback, no HTTP requests or page errors, and 390px mobile overflow. Run `node browser.test.cjs` with Playwright and optional `BLOOM_CHROMIUM`. Desktop/mobile screenshots were inspected. The original specimen and exact code-point recovery are preserved. Dense marks use the host's available font coverage.

September 11, 2026: README and workshop copy now foreground the letter-and-weather design; the original source checksum manifest remains the recovery baseline. The source specimen, tables and codec behavior are unchanged.
